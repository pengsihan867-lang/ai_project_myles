# RouteAlpha M1 依赖
# 核心管线(必须)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
pyyaml>=6.0
loguru>=0.7
pyarrow>=14.0        # 写/读 predictions.parquet
matplotlib>=3.7      # test.ipynb 可视化

# MILP 求解器(预算约束路由)。学术/试用 license 即可解本项目规模。
gurobipy>=11.0

# 特征编码(bge-small)。未安装时 ml_seperate.py 自动回退 TF-IDF, 不影响跑通。
# 首次使用需联网下载模型权重。
sentence-transformers>=2.2
tf-keras>=2.15          # 部分环境 transformers 导入需要, 避免 Keras3 冲突
torch>=2.0              # sentence-transformers 后端

# 可选: 免费替代 Gurobi 的求解器(本仓库默认用 Gurobi, 如需可自行切换)
# ortools>=9.7
